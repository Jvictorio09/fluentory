# Local Development Environment Variables
# Copy this file to .env and update with your local values

# Database Configuration
# For PostgreSQL (recommended - same as production):
# Format: postgresql://[user[:password]@][host][:port][/database]
# Example with password: postgresql://postgres:postgres@localhost:5432/fluentory_dev
# Example without password: postgresql://postgres@localhost:5432/fluentory_dev
DATABASE_URL=postgresql://postgres@localhost:5432/fluentory_dev

# Optional: For SQLite (not recommended due to migration incompatibilities):
# DATABASE_URL=sqlite:///db.sqlite3
